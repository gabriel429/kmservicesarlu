name: Vercel deploy disabled

# Vercel deployment workflow intentionally removed — this repository is
# configured for self-hosting (Hostinger). If you later need CI/CD,
# create an appropriate workflow for your host.
